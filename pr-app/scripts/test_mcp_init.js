import { fetchEventSource } from '@microsoft/fetch-event-source';

async function test() {
    const url = "https://unblusterously-spathic-bryan.ngrok-free.dev";

    // Polyfill EventSource for fetchEventSource test if needed or just use standard fetch for HTTP POST
    // Actually we can just write a native node script using fetch.
}
